- Implement early stop
- Plot training and valid loss
- Plot learning rate per epoch
- Change Grad-CAM visualization: gc + original image
- Save config in experiment dir
- Test sigmoid after Linear in Grad-CAM to get probabilities
  instead of softmax directly